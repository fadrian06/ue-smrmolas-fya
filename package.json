{
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^5.15.4",
    "esbuild": "^0.27.2",
    "sass": "^1.97.3"
  },
  "scripts": {
    "build": "sass -q resources/scss:resources/build & esbuild resources/ts/**/*.ts --outdir=resources/build --bundle --sourcemap"
  }
}
